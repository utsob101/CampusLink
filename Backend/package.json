{
  "name": "campuslink-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --ignore server-config.json --ignore uploads/ src/server.js",
    "lint": "echo 'No lint configured' && exit 0",
    "test": "node -e \"console.log('No tests yet')\""
  },
  "nodemonConfig": {
    "ignore": [
      "server-config.json",
      "uploads/*",
      "../CampusLink-main/server-config.json"
    ]
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
